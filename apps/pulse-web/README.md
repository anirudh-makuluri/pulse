# Pulse web

The landing page for Pulse, the activity layer for your work.

## Development

Requires Node.js 22.13 or newer.

```bash
npm install
npm run dev
```

Create a production build with:

```bash
npm run build
```

The primary download button points to the latest Windows installer published
from the main Pulse repository.
